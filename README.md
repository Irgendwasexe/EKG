# Bild-Webseite

Diese Website zeigt ein Bild aus einem konfigurierbaren Ordner.

## So geht's
1. Lege Bilder in den Ordner `images` ab.
2. Bearbeite `config.json` und setze `defaultImage` auf den gewünschten Dateinamen.
3. Öffne `index.html` im Browser.
